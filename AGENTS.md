# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

## Repository rules

- TypeScript interface files must be kept in a separate folder.
- Magic numbers and constants that do not change must be kept in a separate folder.
